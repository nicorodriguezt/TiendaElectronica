@model IEnumerable<TiendaElectronica.lineapresupuesto>

@{
    ViewBag.Title = "Index";
}

<h2>Componentes</h2>

<p>
    @Html.ActionLink("Agregar Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PrecioUnit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.presupuesto.idPresupuesto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.producto.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    if (item.idPresupuesto == ViewBag.idPresupuesto)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PrecioUnit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.presupuesto.idPresupuesto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.producto.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.idLineaPresupuesto }) |
            @Html.ActionLink("Details", "Details", new { id = item.idLineaPresupuesto }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.idLineaPresupuesto })
        </td>
    </tr>
    }
}

</table>
